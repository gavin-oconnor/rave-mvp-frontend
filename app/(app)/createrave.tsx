import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const createrave = () => {
  return (
    <View>
      <Text>createrave</Text>
    </View>
  );
};

export default createrave;

const styles = StyleSheet.create({});
